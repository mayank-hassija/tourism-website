# 💡 Node.js dependencies
backend/node_modules/
frontend/node_modules/

# 🚫 Build outputs
frontend/dist/
backend/dist/

# 🔐 Environment variables (keep secrets out of version control)
backend/.env
frontend/.env

# 🧹 System & editor clutter
.DS_Store         # macOS file system metadata
*.log             # Log files (e.g., npm-debug.log)
*.env.*.local     # Optional local override envs

# 📦 Vite cache (optional, but can speed up clean clones)
frontend/.vite/
